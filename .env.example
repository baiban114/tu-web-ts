# Development option A:
# Leave VITE_API_BASE_URL unset and let Vite proxy /api to the backend target below.
VITE_DEV_PROXY_TARGET=http://localhost:18080

# Development/production option B:
# If you want the frontend to call a full backend origin directly, uncomment this.
# VITE_API_BASE_URL=http://localhost:18080
